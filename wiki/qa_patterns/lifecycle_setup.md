# Lifecycle Setup

Lifecycle management pattern used in generated test projects. The lifecycle setup (app startup/shutdown, mock server management) is generated from scratch during test generation — it is NOT extracted from golden source files.
